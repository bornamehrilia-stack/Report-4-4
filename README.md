# Report-4-4



---

# 🎯 گزارش نهایی: سیستم کنترل هوشمند نور با سنسور فاصله (Smart Light Control with Ultrasonic Sensor)

## ۱. مقدمه  
در این پروژه، یک **سنسور HC-SR04** به آردوینو متصل شده است و فاصله‌ی اجسام را اندازه‌گیری می‌کند. سپس این فاصله به صورت **پویا و مستقیم** بر روی شدت نور یک LED تأثیر می‌گذارد — یعنی هرچه جسم نزدیک‌تر باشد، LED روشن‌تر می‌شود و هرچه دورتر باشد، خاموش‌تر می‌شود. 



## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- سنسور HC-SR04  
- یک LED (هر رنگی)  
- یک مقاومت ۲۲۰ اهم  
- برد برد (Breadboard)  
- سیم‌های جامپر (jumper wires)

> ✅ این پروژه نیاز به سنسور LED و مقاومت دارد.

## ۴. نحوه‌ی ساخت مدار  
- **سنسور HC-SR04**:  
  - VCC → 5V آردوینو  
  - GND → GND آردوینو  
  - TRIG → پین ۱۱ آردوینو  
  - ECHO → پین ۱۲ آردوینو  
- **LED**:  
  - پایه‌ی مثبت → **پین دیجیتال ۵** آردوینو (پین PWM — با نماد ~)  
  - پایه‌ی منفی → از طریق مقاومت ۲۲۰ اهم → **GND**

.

## ۵. کد برنامه (اسکچ):

```cpp
int trig = 11;     // تعریف پین TRIG
int echo = 12;     // تعریف پین ECHO
int duration;      // متغیر برای ذخیره زمان سفر موج
int distances;     // متغیر برای ذخیره فاصله
int led = 5;       // تعریف پین ۵ به عنوان خروجی LED

void setup() {
  Serial.begin(9600);   // شروع ارتباط سریال برای نمایش داده
  pinMode(trig, OUTPUT); // تنظیم پین TRIG به عنوان خروجی
  pinMode(echo, INPUT);  // تنظیم پین ECHO به عنوان ورودی
  pinMode(led, OUTPUT);  // تنظیم پین ۵ به عنوان خروجی
}

void loop() {
  // ارسال پالس تحریک (TRIG)
  digitalWrite(trig, LOW);
  delayMicroseconds(2);        // تأخیر ۲ میکروثانیه
  digitalWrite(trig, HIGH);
  delayMicroseconds(10);       // تأخیر ۱۰ میکروثانیه
  digitalWrite(trig, LOW);

  // دریافت بازتاب (ECHO)
  duration = pulseIn(echo, HIGH);  // اندازه‌گیری طول پالس بازتاب

  // محاسبه فاصله (به سانتی‌متر)
  distances = (duration / 2) * 0.0343;

  // نمایش فاصله در Serial Monitor
  Serial.println(distances);

  // تبدیل فاصله به مقدار PWM (۰ تا ۲۵۵)
  int new_val = map(distances, 30, 4, 0, 255);

  // تنظیم شدت نور LED
  if (new_val >= 4 && new_val <= 30) {
    analogWrite(led, new_val);
  } else {
    analogWrite(led, 0);  // اگر فاصله خارج از محدوده باشد، LED خاموش شود
  }

  delay(20);  // تأخیر کوتاه برای ثبات کار
}
```

### 🔍 توضیح کد:  
- `digitalWrite(trig, LOW)` → شروع ارسال پالس تحریک.  
- `delayMicroseconds(2)` → تأخیر ۲ میکروثانیه برای پایداری.  
- `digitalWrite(trig, HIGH)` → ارسال پالس تحریک.  
- `delayMicroseconds(10)` → تأخیر ۱۰ میکروثانیه برای ارسال کامل پالس.  
- `digitalWrite(trig, LOW)` → پایان ارسال پالس تحریک.  
- `pulseIn(echo, HIGH)` → اندازه‌گیری طول پالس بازتاب (زمان سفر موج صوتی).  
- `distances = (duration / 2) * 0.0343` → محاسبه فاصله:
  - تقسیم بر ۲ چون موج رفت و برگشت می‌کند.
  - ضرب در ۰.۰۳۴۳ چون سرعت صوت در هوا ≈ ۳۴۳ متر بر ثانیه است (یعنی ۰.۰۳۴۳ سانتی‌متر در میکروثانیه).
- `map(distances, 30, 4, 0, 255)` → تبدیل محدوده‌ی فاصله (۴ تا ۳۰ سانتی‌متر) به محدوده‌ی PWM (۰ تا ۲۵۵).  
- `if (new_val >= 4 && new_val <= 30)` → فقط اگر فاصله در محدوده باشد، LED روشن شود.  
- `analogWrite(led, new_val)` → تنظیم شدت نور LED بر اساس فاصله.

## ۶. نحوه‌ی کارکرد  
- پس از آپلود کد، سنسور شروع به ارسال موج صوتی می‌کند.  
- وقتی شما دست یا جسمی را نزدیک یا دور سنسور حرکت دهید، فاصله تغییر می‌کند.  
- LED به صورت پیوسته و بدون جهش، شدت نور خود را تغییر می‌دهد:
  - وقتی جسم نزدیک است (مثلاً ۴ سانتی‌متر) → LED کاملاً روشن می‌شود.
  - وقتی جسم دور است (مثلاً ۳۰ سانتی‌متر) → LED خاموش می‌شود.
  - در فواصل میانی → LED با شدت متوسط روشن می‌شود.

## ۷. نتایج  
LED به صورت پیوسته و بدون جهش، با تغییر فاصله، شدت نور خود را تغییر می‌دهد. 


